import Layout from '../components/layout';
import { type NextPage } from 'next';

const HTMLPage: NextPage = () => {
  return <Layout sidebar={true} hideAd={true} hideSidebar={true}>HTML PAGE vnvnnvmvmvm</Layout>;
};

export default HTMLPage;
